FROM texlive/texlive:latest

# Examples of customization (uncomment and modify as needed):
#
# 1. Install additional LaTeX packages via tlmgr:
# RUN tlmgr update --self && tlmgr install <package-name>
#
# 2. Change default Japanese fonts:
# RUN kanji-config-updmap-sys ipaex
